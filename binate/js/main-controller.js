'use strict';

app.controller('mainCtrl', ['$rootScope', '$document', '$timeout', '$scope', function ($rootScope, $document, $timeout, $scope) {
 
//Page scroll
app.value('duScrollDuration', 800)
app.value('duScrollBottomSpy', true);
app.value('duScrollOffset', 30);

}]);
